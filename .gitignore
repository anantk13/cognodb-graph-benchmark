# Secrets — never commit connection URIs or passwords
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
.pytest_cache/
.ruff_cache/

# Datasets — regenerated by `make data`, too large to track
data/raw/
data/build/

# Raw benchmark output — large; the committed results live in results/published/
results/raw/

# Kuzu embedded database files
*.kuzu/

# OS
.DS_Store
